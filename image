import matplotlib.pyplot as plt
import matplotlib.image as mpimg


def picture():
  state = input("state?:")
  if state == "ALABAMA":
    img = mpimg.imread('ALoutline.jpeg')
    imgplot = plt.imshow(img)
    imgplot.show()
  else:
  print('ok')
picture()
